name: CI
on: [push, pull_request]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Test1
      run: |
        ls -al ~/.cargo/registry/
        rustup install beta-x86_64-unknown-linux-gnu
        cd foo
        cargo +beta-x86_64-unknown-linux-gnu fetch
