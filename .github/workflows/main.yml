name: CI
on: [push, pull_request]

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # build: [windows]
        include:
          # - build: stable
          #   os: ubuntu-latest
          #   rust: stable
          # - build: beta
          #   os: ubuntu-latest
          #   rust: beta
          # - build: nightly
          #   os: ubuntu-latest
          #   rust: nightly
          # - build: macos
          #   os: macos-latest
          #   rust: stable
          - os: windows-latest
          #   rust: stable
    steps:
    - uses: actions/checkout@master
    - run: |
        mkdir -p /tmp/awscli-deps
        curl https://ci-mirrors.rust-lang.org/rustc/2019-07-27-awscli.tar | tar xf - -C /tmp/awscli-deps
        pip --version
        pip install --no-index "--find-links=/tmp/awscli-deps" awscli
      shell: bash


  # test1:
  #   name: Test1
  #   runs-on: windows-latest
  #   steps:
  #   - uses: actions/checkout@master
  #   - name: Test1
  #     shell: bash
  #     run: |
  #       cd foo
  #       cargo -V
  #       cargo run
