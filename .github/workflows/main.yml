name: CI
on: [push, pull_request]

jobs:
  test1:
    name: Test1
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@master
    - name: Test1
      shell: bash
      run: |
        set -x
        env
        which lldb
        rm /c/Program\ Files/LLVM/bin/lldb.exe
        lldb --version || true
        lldb -P || true
